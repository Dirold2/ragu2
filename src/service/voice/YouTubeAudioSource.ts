// import { AudioSource, TrackInfo } from './AudioSource';

// export class YouTubeAudioSource implements AudioSource {
//     async getAudioStream(url: string): Promise<NodeJS.ReadableStream> {
//         // Реализация для YouTube
//     }

//     async getTrackInfo(url: string): Promise<TrackInfo> {
//         // Реализация для YouTube
//     }
// }
